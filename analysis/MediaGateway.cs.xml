<?xml version="1.0" encoding="utf-8"?>
<AnalysisReport>
  <FilePath>Core\Media\Gateway\MediaGateway.cs</FilePath>
  <OriginalWarnings>
    <Warning>
      <Code>ASP0016</Code>
      <Message>The method used to create a RequestDelegate returns Task&lt;object?&gt;. RequestDelegate discards this value. If this isn't intended then change the return type to non-generic Task or, if the delegate is a route handler, cast it to Delegate so the return value is written to the response.</Message>
      <Line>64</Line>
      <Column>55</Column>
    </Warning>
    <Warning>
      <Code>ASP0016</Code>
      <Message>The method used to create a RequestDelegate returns Task&lt;object?&gt;. RequestDelegate discards this value. If this isn't intended then change the return type to non-generic Task or, if the delegate is a route handler, cast it to Delegate so the return value is written to the response.</Message>
      <Line>67</Line>
      <Column>57</Column>
    </Warning>
    <Warning>
      <Code>ASP0016</Code>
      <Message>The method used to create a RequestDelegate returns Task&lt;object?&gt;. RequestDelegate discards this value. If this isn't intended then change the return type to non-generic Task or, if the delegate is a route handler, cast it to Delegate so the return value is written to the response.</Message>
      <Line>70</Line>
      <Column>55</Column>
    </Warning>
    <Warning>
      <Code>ASP0016</Code>
      <Message>The method used to create a RequestDelegate returns Task&lt;object?&gt;. RequestDelegate discards this value. If this isn't intended then change the return type to non-generic Task or, if the delegate is a route handler, cast it to Delegate so the return value is written to the response.</Message>
      <Line>73</Line>
      <Column>54</Column>
    </Warning>
  </OriginalWarnings>
  <FixesApplied>
    <Fix>
      <Description>Cast lambda expressions to Delegate to resolve ASP0016 warnings about RequestDelegate return type mismatch</Description>
      <CodeChanged>
        <![CDATA[
        endpoints.MapGet("/localmedia/images/random", (HttpContext httpContext) =>
            HandleRandom(httpContext, "images/random", provider => provider.GetRandomImageAsync(httpContext.Request, httpContext.RequestAborted)));

        endpoints.MapGet("/localmedia/pictures/random", (HttpContext httpContext) =>
            HandleRandom(httpContext, "pictures/random", provider => provider.GetRandomImageAsync(httpContext.Request, httpContext.RequestAborted)));

        endpoints.MapGet("/localmedia/videos/random", (HttpContext httpContext) =>
            HandleRandom(httpContext, "videos/random", provider => provider.GetRandomVideoAsync(httpContext.Request, httpContext.RequestAborted)));

        endpoints.MapGet("/localmedia/video/random", (HttpContext httpContext) =>
            HandleRandom(httpContext, "video/random", provider => provider.GetRandomVideoAsync(httpContext.Request, httpContext.RequestAborted)));
        ]]>
      </CodeChanged>
      <CodeFixed>
        <![CDATA[
        endpoints.MapGet("/localmedia/images/random", (Delegate)(HttpContext httpContext) =>
            HandleRandom(httpContext, "images/random", provider => provider.GetRandomImageAsync(httpContext.Request, httpContext.RequestAborted)));

        endpoints.MapGet("/localmedia/pictures/random", (Delegate)(HttpContext httpContext) =>
            HandleRandom(httpContext, "pictures/random", provider => provider.GetRandomImageAsync(httpContext.Request, httpContext.RequestAborted)));

        endpoints.MapGet("/localmedia/videos/random", (Delegate)(HttpContext httpContext) =>
            HandleRandom(httpContext, "videos/random", provider => provider.GetRandomVideoAsync(httpContext.Request, httpContext.RequestAborted)));

        endpoints.MapGet("/localmedia/video/random", (Delegate)(HttpContext httpContext) =>
            HandleRandom(httpContext, "video/random", provider => provider.GetRandomVideoAsync(httpContext.Request, httpContext.RequestAborted)));
        ]]>
      </CodeFixed>
    </Fix>
  </FixesApplied>
  <Status>Fixed</Status>
  <Timestamp>2026-02-04T12:00:00Z</Timestamp>
</AnalysisReport>